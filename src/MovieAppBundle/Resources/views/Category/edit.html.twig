{% extends '::adminbase.html.twig' %}

{% block body -%}
    <h1>Category edit  <a href="{{ path('category') }}" class="btn-primary btn pull-right">
            Back to the list
        </a></h1><br/>
    {{ form(edit_form) }}
    <div>
        {{ form(delete_form) }}
    </div>


{% endblock %}
