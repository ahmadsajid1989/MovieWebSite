{% extends '::adminbase.html.twig' %}

{% block body -%}
    <div class="row">
        <div class="col-md-8">
            <h1>Movie edit  <a href="{{ path('movies') }}" class="btn-primary btn pull-right">
                    Back to the list
                </a></h1>

            {{ form(edit_form) }}
            {{ form(delete_form) }}
        </div>
    </div>

{% endblock %}
