{% extends '::adminbase.html.twig' %}

{% block body -%}
    <div class="row">
        <div class="col-md-8">
            <h1>Genre  <a href="{{ path('genre') }}" class="btn btn-primary pull-right">
                    Back to the list
                </a></h1>

            <table class="record_properties table-bordered table table-striped">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ entity.name }}</td>
                </tr>
                <tr>
                    <th>Action</th>
                    <td> <a href="{{ path('genre_edit', { 'id': entity.id }) }}">
                            Edit
                        </a></td>
                </tr>
                </tbody>
            </table>
            {{ form(delete_form) }}
        </div>
    </div>
{% endblock %}
