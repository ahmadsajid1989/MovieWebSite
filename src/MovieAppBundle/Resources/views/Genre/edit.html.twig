{% extends '::adminbase.html.twig' %}

{% block body -%}
    <div class="row">
        <div class="col-md-8">
            <h1>Edit Genre  <a href="{{ path('genre') }}" class="btn btn-primary pull-right">
                    Back to the list
                </a></h1>
<br/>
            {{ form(edit_form) }}
            {{ form(delete_form) }}
        </div>
    </div>
{% endblock %}
